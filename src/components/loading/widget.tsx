import React from 'react';

const LoadingWidget: React.FunctionComponent<any> = () => (
  <div className="loading-widget-wrapper h-40 flex justify-center items-center">
    <span>Loading...</span>
  </div>
);

export default LoadingWidget;
